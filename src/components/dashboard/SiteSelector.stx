<script client lang="ts">
/**
 * SiteSelector Component - Site selection and creation
 * Note: All functionality is handled by dashboard.ts (fetchSites, renderSiteSelector, etc.)
 * This component just provides the HTML structure and styles.
 */
</script>

<div id="site-selector" class="flex flex-col items-center justify-center min-h-screen p-8 text-center" style="display:none">
  <h1 class="text-3xl font-bold text-text mb-2">Analytics Dashboard</h1>
  <p class="text-text-2 mb-8">Select a site to view analytics</p>

  <div id="site-list" class="flex flex-col items-center gap-4 w-full max-w-[600px]">
    <!-- Content populated by renderSiteSelector() in dashboard.ts -->
    <div class="loading-state flex items-center gap-2 text-muted p-8">
      <div class="spinner"></div>
      <span>Loading sites...</span>
    </div>
  </div>
</div>

<style>
/* Animation keyframes - cannot be expressed as utility classes */
.spinner {
  width: 16px;
  height: 16px;
  border: 2px solid var(--border);
  border-top-color: var(--accent);
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
}
@keyframes spin {
  to { transform: rotate(360deg); }
}

/* Dynamic classes used by dashboard.ts renderSiteSelector() */
.site-card {
  @apply flex items-center gap-4 w-full py-4 px-4 bg-bg-2 border border-border rounded-lg cursor-pointer text-left mb-2 transition-all duration-200 text-text;
}
.site-card:hover {
  @apply border-accent bg-bg-3;
  transform: translateY(-1px);
}
.site-icon {
  @apply w-10 h-10 bg-bg-3 rounded-lg flex items-center justify-center shrink-0 text-accent;
}
.site-info {
  @apply flex-1 flex flex-col gap-1 min-w-0;
}
.site-name {
  @apply text-base font-semibold text-text;
}
.site-domain {
  @apply text-xs text-muted;
}
.arrow {
  @apply text-muted shrink-0;
}
.site-card:hover .arrow {
  color: var(--accent);
}
.empty {
  @apply text-muted p-8;
}
</style>
