<script server>
export const title: string = props.title || 'Analytics Dashboard'
export const containerClass: string = props.containerClass || 'max-w-6xl'
export const apiEndpoint: string = props.apiEndpoint || ''
export const siteId: string = props.siteId || ''
</script>

<!DOCTYPE html>
<html lang="en" class="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>
</head>
<body class="bg-gray-900 text-gray-200 font-sans antialiased min-h-screen p-8">
  <div class="{{ containerClass }} mx-auto">
    @yield('content')
  </div>

  <script>
    window.ANALYTICS_API = '{{ apiEndpoint }}'
    window.ANALYTICS_SITE_ID = '{{ siteId }}'
  </script>
  @stack('scripts')
</body>
</html>
