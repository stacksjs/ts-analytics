// Re-export of the canonical analytics types (single source of truth: src/types).
// Components in resources/ import from `../types/analytics` / `../../types/analytics`.
export type * from '../../src/types/analytics'
