// Re-export of the canonical dashboard UI types (single source of truth: src/types).
// Components in resources/ import from `../types/dashboard` / `../../types/dashboard`.
export type * from '../../src/types/dashboard'
