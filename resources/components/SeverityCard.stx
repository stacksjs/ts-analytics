<script client>
/**
 * SeverityCard Component
 * Displays a severity count badge
 */
const severity: string = $props.severity || 'medium'
const count: number = $props.count || 0
const label: string = $props.label || ''
</script>

<div @class="['py-3 px-4 rounded-lg text-center min-w-[80px] severity-card', severity]">
  <div class="text-xl font-semibold text-text">{{ count }}</div>
  <div class="text-[0.6875rem] text-muted uppercase">{{ label }}</div>
</div>

<style>
.severity-card.critical {
  background: rgba(239, 68, 68, 0.1);
  border: 1px solid rgba(239, 68, 68, 0.2);
}
.severity-card.high {
  background: rgba(249, 115, 22, 0.1);
  border: 1px solid rgba(249, 115, 22, 0.2);
}
.severity-card.medium {
  background: rgba(234, 179, 8, 0.1);
  border: 1px solid rgba(234, 179, 8, 0.2);
}
.severity-card.low {
  background: rgba(107, 114, 128, 0.1);
  border: 1px solid rgba(107, 114, 128, 0.2);
}
</style>
