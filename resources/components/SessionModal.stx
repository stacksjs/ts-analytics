<script server>
/**
 * SessionModal Component
 *
 * Provides hidden DOM templates used by client-side code to render session
 * detail modals (heatmap, journey, timeline). Takes no props.
 */
</script>

<template>
<div id="session-modal-template" class="stx-template" hidden>
  <div class="modal-content max-w-[1000px]">
    <div class="modal-header">
      <h3 class="text-base font-semibold m-0 text-text">Session: <span id="session-id"></span></h3>
      <button class="icon-btn" @click="window.closeModal()">
        <Icon name="x" size="20" />
      </button>
    </div>
    <div class="modal-body">
      <div class="grid grid-cols-4 gap-4 mb-6" id="session-stats"></div>
      <div id="session-heatmap-section" style="display:none">
        <h4 class="mb-3 text-sm text-muted font-medium">Click Heatmap (<span id="clicks-count">0</span> clicks)</h4>
        <div id="heatmap-path-buttons" class="flex gap-2 mb-4 flex-wrap"></div>
        <div id="heatmap-container" class="heatmap-container">
          <div id="heatmap-clicks" class="absolute inset-0"></div>
          <div id="heatmap-viewport" class="absolute bottom-2.5 right-2.5 text-[0.6875rem] text-muted bg-bg-2 py-1 px-2 rounded"></div>
        </div>
      </div>
      <h4 class="mb-3 text-sm text-muted font-medium">Session Journey</h4>
      <div id="session-journey" class="flex items-center gap-2 mb-6 flex-wrap"></div>
      <h4 class="mb-3 text-sm text-muted font-medium">Timeline (<span id="timeline-count">0</span> events)</h4>
      <div id="session-timeline" class="max-h-[300px] overflow-y-auto flex flex-col gap-2"></div>
    </div>
  </div>
</div>

<div id="session-stat-template" class="stx-template" hidden>
  <div class="stat-card text-center">
    <div class="text-xl font-bold text-text"></div>
    <div class="text-[0.6875rem] text-muted uppercase tracking-wide mt-1"></div>
  </div>
</div>

<div id="journey-step-template" class="stx-template" hidden>
  <div class="bg-bg border border-border py-2 px-3 rounded-md text-xs">
    <div class="text-text"></div>
    <div class="text-muted text-[0.6875rem]"></div>
  </div>
</div>

<div id="timeline-item-template" class="stx-template" hidden>
  <div class="flex items-center gap-3 p-2 bg-bg rounded">
    <div class="timeline-type"></div>
    <div class="flex-1 text-[0.8125rem] text-text overflow-hidden text-ellipsis whitespace-nowrap"></div>
    <div class="text-[0.6875rem] text-muted whitespace-nowrap"></div>
  </div>
</div>

<div id="timeline-pageview-content" class="stx-template" hidden>
  <span class="text-accent"></span>
</div>

<div id="timeline-event-content" class="stx-template" hidden>
  <span class="text-success"></span>
</div>

<div id="timeline-click-content" class="stx-template" hidden>
  <span class="text-text"></span> <code class="bg-bg-2 py-0.5 px-1 rounded-sm text-xs"></code>
</div>

<div id="timeline-vital-content" class="stx-template" hidden>
  <span class="text-warning"></span>: <span class="text-muted"></span>
</div>

<div id="timeline-error-content" class="stx-template" hidden>
  <span class="text-error"></span>
</div>
</template>
<style>
.heatmap-container {
  position: relative;
  width: 100%;
  height: 400px;
  background: var(--bg);
  border: 1px solid var(--border);
  border-radius: 8px;
  overflow: hidden;
  margin-bottom: 1.5rem;
}

.timeline-type {
  font-size: 0.6875rem;
  font-weight: 500;
  text-transform: uppercase;
  padding: 0.125rem 0.375rem;
  border-radius: 4px;
  background: var(--bg2);
  color: var(--muted);
  min-width: 60px;
  text-align: center;
}
.timeline-type.pageview { background: rgba(59, 130, 246, 0.15); color: #3b82f6; }
.timeline-type.event { background: rgba(34, 197, 94, 0.15); color: #22c55e; }
.timeline-type.click { background: rgba(168, 85, 247, 0.15); color: #a855f7; }
.timeline-type.vital { background: rgba(245, 158, 11, 0.15); color: #f59e0b; }
.timeline-type.error { background: rgba(239, 68, 68, 0.15); color: #ef4444; }

.journey-arrow {
  color: var(--muted);
}
</style>
